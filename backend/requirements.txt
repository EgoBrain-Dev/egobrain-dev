Flask==3.1.2              # Framework web principal - cria API e rotas
flask-cors==6.0.1         # Permite frontend fazer requests para o backend (CORS)
python-decouple==3.8      # Gerencia configurações sensíveis (variáveis de ambiente)